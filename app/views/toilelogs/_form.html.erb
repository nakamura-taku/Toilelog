<%= form_with(model: toilelog, local: true) do |form| %>
  <% if toilelog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toilelog.errors.count, "error") %> prohibited this toilelog from being saved:</h2>

      <ul>
      <% toilelog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :toilelog_title %>
  </div>

  <div class="field">
    <%= form.label :kuchikomi %>
    <%= form.text_field :kuchikomi, id: :toilelog_kuchikomi %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
